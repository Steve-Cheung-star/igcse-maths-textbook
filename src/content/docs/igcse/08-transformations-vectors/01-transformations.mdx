---
title: 8.1 Transformations
description: Recognise, describe, and draw reflections, rotations, enlargements, and translations, including combined and inverse transformations for IGCSE 0607.
---

import TransformationLab from '../../../../components/TransformationLab.jsx';

A transformation changes the position, size, or orientation of a shape. The original shape is called the **Object**, and the new shape is called the **Image** (often denoted with a prime symbol, e.g., $A'$). 

<Aside type="note" title="Transformation Anatomy">

  Transformations can be broadly categorised into two anatomical families:
  1. **Isometric Transformations:** These preserve the exact size and shape of the object. The object and image are congruent.
     * Reflection (Flip)
     * Rotation (Turn)
     * Translation (Slide)
  2. **Non-Isometric Transformations:** These change the size of the object. The object and image are mathematically similar, but not congruent.
     * Enlargement (Grow/Shrink)

</Aside>

<SteveTip title="The Golden Exam Rule">

  Always use a straight edge or ruler to draw the edges of your shapes, projection lines, and mirror lines in the exam. Furthermore, if a question asks for a "single transformation", you will score zero if you list two (e.g., "a translation and a reflection").

</SteveTip>

---

## 1. Reflection

A reflection is a "flip" of a shape across a straight line, acting as a mirror. Questions can feature horizontal lines ($y = c$), vertical lines ($x = c$), or diagonal lines ($y = x$ or $y = -x$).

<div class="not-content">

  <TransformationLab category="reflection" client:only="react" />

</div>

<Aside type="tip" title="Properties & Inverse of Reflection">

  * **To describe fully:** You must state the word "Reflection" and the equation of the mirror line (e.g., $x = 0$, $y = 2$, $y = -x$).
  * **Key Fact:** Every point on the image is the exact same perpendicular distance from the mirror line as the corresponding point on the object.
  * **The Inverse:** The inverse of a reflection is **exactly the same reflection**. Flipping an image back over the same mirror line returns it to the object's position.

</Aside>

---

## 2. Rotation

A rotation turns a shape around a fixed pivot point. This point does not have to be the origin $(0,0)$; it can be anywhere on the grid!

<div class="not-content">

  <TransformationLab category="rotation" client:only="react" />

</div>

<Aside type="tip" title="Properties & Inverse of Rotation">

  * **To describe fully, you must state 3 things:**
    1.  **Transformation type:** "Rotation"
    2.  **Centre of rotation:** Given as a coordinate $(x, y)$.
    3.  **Angle & Direction:** $90^\circ$ clockwise, $90^\circ$ anticlockwise, or $180^\circ$.
  * **The Inverse:** To reverse a rotation, keep the same centre and angle, but **reverse the direction**. For example, the inverse of $90^\circ$ clockwise is $90^\circ$ anticlockwise. (The inverse of $180^\circ$ is just another $180^\circ$ turn).

</Aside>

<Aside type="caution" title="Direction for 180°">

  A common misconception is thinking you must provide a direction for a $180^\circ$ rotation. Direction is not needed for $180^\circ$ rotations, because turning halfway clockwise or halfway anticlockwise lands you in the exact same position!

</Aside>

---

## 3. Enlargement

An enlargement changes the size of the shape, altering the side lengths. The centre of enlargement acts as a "projector" lens from which the shape grows or shrinks.

<div class="not-content">

  <TransformationLab category="enlargement" client:only="react" />

</div>

<Aside type="tip" title="Properties & Inverse of Enlargement">

  * **To describe fully, you must state 3 things:**
    1.  **Transformation type:** "Enlargement"
    2.  **Centre of enlargement:** The coordinate $(x, y)$.
    3.  **Scale Factor (SF):** How many times bigger the shape gets.
  * **The Inverse:** Keep the same centre, but use the **reciprocal of the scale factor**. If a shape was enlarged by a scale factor of $3$, the inverse transformation is an enlargement by a scale factor of $\frac{1}{3}$. 

</Aside>

**Scale Factor Rules:**
* **Positive Integer ($SF > 1$):** Shape gets bigger and moves further from the centre.
* **Fractional ($0 < SF < 1$):** Shape gets smaller and moves closer to the centre.
* **Negative ($SF < 0$):** Shape passes *through* the centre of enlargement, gets flipped upside down, and ends up on the opposite side.

---

## 4. Translation

A translation is a straight slide. The shape is not rotated, flipped, or resized.

<Aside type="tip" title="Properties & Inverse of Translation">

  * **To describe fully:** State the word "Translation" and provide the column vector:
  $$
  \begin{pmatrix} x \\ y \end{pmatrix}
  $$
    * **Top number ($x$):** Movement Left/Right (positive = right, negative = left).
    * **Bottom number ($y$):** Movement Up/Down (positive = up, negative = down).
  * **The Inverse:** Simply **invert the signs** of the vector. The inverse of $\begin{pmatrix} -4 \\ 3 \end{pmatrix}$ is $\begin{pmatrix} 4 \\ -3 \end{pmatrix}$. 

</Aside>

<div class="not-content">

  <TransformationLab category="translation" client:only="react" />

</div>

---

## Practice Questions and Examples

<Tabs>

  <TabItem label="📝 Q1: Identifying Diagonal Reflection">
    Describe fully the single transformation that maps Triangle $A$ onto Triangle $A'$.
    
    <div style={{ display: 'flex', justifyContent: 'center', margin: '1rem 0' }}>
      <svg viewBox="-100 -100 200 200" width="100%" maxWidth="300px" style={{ background: '#f8fafc', borderRadius: '8px' }}>
        <g stroke="#e2e8f0" stroke-width="1">
          {Array.from({length: 11}).map((_, i) => <line key={`x-${i}`} x1="-100" y1={-100 + i*20} x2="100" y2={-100 + i*20} />)}
          {Array.from({length: 11}).map((_, i) => <line key={`y-${i}`} x1={-100 + i*20} y1="-100" x2={-100 + i*20} y2="100" />)}
        </g>
        <line x1="-100" y1="0" x2="100" y2="0" stroke="#1e293b" stroke-width="2" />
        <line x1="0" y1="-100" x2="0" y2="100" stroke="#1e293b" stroke-width="2" />
        
        {/* Object A: (1, 2), (3, 2), (1, 5) -> SVG coords: x*20, -y*20 */}
        <polygon points="20,-40 60,-40 20,-100" fill="#eff6ff" stroke="#3b82f6" stroke-width="2" />
        <text x="30" y="-50" font-weight="bold" fill="#3b82f6" text-anchor="middle">A</text>

        {/* Image A': (-2, -1), (-2, -3), (-5, -1) */}
        <polygon points="-40,20 -40,60 -100,20" fill="#f0fdf4" stroke="#10b981" stroke-width="2" />
        <text x="-55" y="45" font-weight="bold" fill="#10b981" text-anchor="middle">A'</text>
      </svg>
    </div>

    a) Identify the general type of transformation shown.
    
    b) Find the equation of the mirror line by connecting corresponding vertices and finding their midpoints.
    
    c) Write the complete mathematical description of the transformation.
    
  </TabItem>

  <TabItem label="✅ Solution 1">
    <div style={{ display: 'flex', justifyContent: 'center', margin: '1rem 0' }}>
      <svg viewBox="-100 -100 200 200" width="100%" maxWidth="300px" style={{ background: '#f8fafc', borderRadius: '8px' }}>
        <g stroke="#e2e8f0" stroke-width="1">
          {Array.from({length: 11}).map((_, i) => <line key={`x-${i}`} x1="-100" y1={-100 + i*20} x2="100" y2={-100 + i*20} />)}
          {Array.from({length: 11}).map((_, i) => <line key={`y-${i}`} x1={-100 + i*20} y1="-100" x2={-100 + i*20} y2="100" />)}
        </g>
        <line x1="-100" y1="0" x2="100" y2="0" stroke="#1e293b" stroke-width="2" />
        <line x1="0" y1="-100" x2="0" y2="100" stroke="#1e293b" stroke-width="2" />
        
        <polygon points="20,-40 60,-40 20,-100" fill="#eff6ff" stroke="#3b82f6" stroke-width="2" opacity="0.4" />
        <polygon points="-40,20 -40,60 -100,20" fill="#f0fdf4" stroke="#10b981" stroke-width="2" />
        
        {/* Invisible lines rendered LAST so they show up clearly on top of the polygons */}
        <line x1="-100" y1="-100" x2="100" y2="100" stroke="#ef4444" stroke-width="2" stroke-dasharray="4,4" />
        <text x="80" y="70" fill="#ef4444" font-size="12" font-weight="bold" text-anchor="middle">y = -x</text>
        <line x1="20" y1="-40" x2="-40" y2="20" stroke="#94a3b8" stroke-dasharray="2,2" stroke-width="1.5" />
      </svg>
    </div>

    <Steps>
      1. **Part a:** Notice the shape has been flipped laterally and inverted, suggesting a diagonal reflection.
      2. **Part b:** Connect corresponding vertices (e.g., top of $A$ to bottom-left of $A'$). Find the midpoint of these connection lines. Draw a line straight through the midpoints. The line passes through $(0,0)$, $(-1,1)$, $(2,-2)$. Its equation is $y = -x$.
      3. **Part c:** **Final Answer:** Reflection in the line $y = -x$.
    </Steps>

  </TabItem>

</Tabs>

<SmartAIGenerator topic="Identifying the mirror line and describing reflections" />

<Tabs>

  <TabItem label="📝 Q2: Rotation around a specific point">
    Rotate Triangle $B$ $90^\circ$ clockwise about the centre $(-1, 1)$.
    
    <div style={{ display: 'flex', justifyContent: 'center', margin: '1rem 0' }}>
      <svg viewBox="-100 -100 200 200" width="100%" maxWidth="300px" style={{ background: '#f8fafc', borderRadius: '8px' }}>
        <g stroke="#e2e8f0" stroke-width="1">
          {Array.from({length: 11}).map((_, i) => <line key={`x-${i}`} x1="-100" y1={-100 + i*20} x2="100" y2={-100 + i*20} />)}
          {Array.from({length: 11}).map((_, i) => <line key={`y-${i}`} x1={-100 + i*20} y1="-100" x2={-100 + i*20} y2="100" />)}
        </g>
        <line x1="-100" y1="0" x2="100" y2="0" stroke="#1e293b" stroke-width="2" />
        <line x1="0" y1="-100" x2="0" y2="100" stroke="#1e293b" stroke-width="2" />
        
        {/* Object B: (-1, 3), (1, 3), (-1, 5) */}
        <polygon points="-20,-60 20,-60 -20,-100" fill="#eff6ff" stroke="#3b82f6" stroke-width="2" />
        <text x="-5" y="-75" font-weight="bold" fill="#3b82f6" text-anchor="middle">B</text>

        {/* Centre (-1, 1) -> SVG (-20, -20) */}
        <circle cx="-20" cy="-20" r="3" fill="#ef4444" />
      </svg>
    </div>

    a) State the coordinates of the centre of rotation.
    
    b) Trace the vector from the centre to the vertex $(-1, 3)$ and apply the $90^\circ$ clockwise rotation. State the new coordinate.
    
    c) Repeat the process for all vertices and draw the final Image $B'$.
    
  </TabItem>

  <TabItem label="✅ Solution 2">
    <div style={{ display: 'flex', justifyContent: 'center', margin: '1rem 0' }}>
      <svg viewBox="-100 -100 200 200" width="100%" maxWidth="300px" style={{ background: '#f8fafc', borderRadius: '8px' }}>
        <g stroke="#e2e8f0" stroke-width="1">
          {Array.from({length: 11}).map((_, i) => <line key={`x-${i}`} x1="-100" y1={-100 + i*20} x2="100" y2={-100 + i*20} />)}
          {Array.from({length: 11}).map((_, i) => <line key={`y-${i}`} x1={-100 + i*20} y1="-100" x2={-100 + i*20} y2="100" />)}
        </g>
        <line x1="-100" y1="0" x2="100" y2="0" stroke="#1e293b" stroke-width="2" />
        <line x1="0" y1="-100" x2="0" y2="100" stroke="#1e293b" stroke-width="2" />
        
        <polygon points="-20,-60 20,-60 -20,-100" fill="#eff6ff" stroke="#3b82f6" stroke-width="2" opacity="0.4" />
        <circle cx="-20" cy="-20" r="3" fill="#ef4444" />
        
        {/* Image B': (1, 1), (1, -1), (3, 1) */}
        <polygon points="20,-20 20,20 60,-20" fill="#f0fdf4" stroke="#10b981" stroke-width="2" />
        <text x="35" y="0" font-weight="bold" fill="#10b981" text-anchor="middle">B'</text>
        
        {/* Path for (-1, 3) to (1, 1) */}
        <path d="M -20 -20 L -20 -60 L 20 -60" fill="none" stroke="#f59e0b" stroke-width="2" stroke-dasharray="2,2" />
        <path d="M -20 -20 L 20 -20 L 20 20" fill="none" stroke="#f59e0b" stroke-width="2" stroke-dasharray="2,2" />
        <path d="M -10 -30 A 14 14 0 0 1 -10 -10" fill="none" stroke="#f59e0b" stroke-width="2" marker-end="url(#arrow-cw)" />
        
        <defs>
          <marker id="arrow-cw" viewBox="0 0 10 10" refX="5" refY="5" marker-width="4" marker-height="4" orient="auto-start-reverse">
            <path d="M 0 0 L 10 5 L 0 10 z" fill="#f59e0b" />
          </marker>
        </defs>
      </svg>
    </div>

    <Steps>
      1. **Part a:** Place your pencil on the centre $(-1, 1)$.
      2. **Part b:** Trace the vector to a corner. To get to $(-1, 3)$, you go **Up 2** from the centre. Rotate that vector $90^\circ$ clockwise. "Up 2" becomes **Right 2**. From the centre $(-1, 1)$, moving Right 2 lands you at $(1, 1)$.
      3. **Part c:** Repeat for the other vertices and connect to form $B'$.
    </Steps>

  </TabItem>

</Tabs>

<SmartAIGenerator topic="Performing a 90-degree clockwise rotation on a coordinate grid" />

<Tabs>

  <TabItem label="📝 Q3: Negative & Fractional Enlargement">
    Enlarge Rectangle $C$ by a Scale Factor of $-\frac{1}{2}$, with Centre $(0,1)$.
    
    <div style={{ display: 'flex', justifyContent: 'center', margin: '1rem 0' }}>
      <svg viewBox="-100 -100 200 200" width="100%" maxWidth="300px" style={{ background: '#f8fafc', borderRadius: '8px' }}>
        <g stroke="#e2e8f0" stroke-width="1">
          {Array.from({length: 11}).map((_, i) => <line key={`x-${i}`} x1="-100" y1={-100 + i*20} x2="100" y2={-100 + i*20} />)}
          {Array.from({length: 11}).map((_, i) => <line key={`y-${i}`} x1={-100 + i*20} y1="-100" x2={-100 + i*20} y2="100" />)}
        </g>
        <line x1="-100" y1="0" x2="100" y2="0" stroke="#1e293b" stroke-width="2" />
        <line x1="0" y1="-100" x2="0" y2="100" stroke="#1e293b" stroke-width="2" />
        
        {/* Object C: (-2, 3), (2, 3), (2, 5), (-2, 5) -> SVG coords */}
        <polygon points="-40,-60 40,-60 40,-100 -40,-100" fill="#eff6ff" stroke="#3b82f6" stroke-width="2" />
        <text x="0" y="-75" font-weight="bold" fill="#3b82f6" text-anchor="middle">C</text>

        {/* Centre (0, 1) -> SVG (0, -20) */}
        <circle cx="0" cy="-20" r="3" fill="#ef4444" />
      </svg>
    </div>

    a) Determine the movement vector from the centre $(0,1)$ to the corner $(-2, 5)$.
    
    b) Apply the Scale Factor of $-\frac{1}{2}$ to this vector and state the new coordinates for this vertex.
    
    c) Repeat this step for the remaining corners and draw the image $C'$.
    
  </TabItem>

  <TabItem label="✅ Solution 3">
    <div style={{ display: 'flex', justifyContent: 'center', margin: '1rem 0' }}>
      <svg viewBox="-100 -100 200 200" width="100%" maxWidth="300px" style={{ background: '#f8fafc', borderRadius: '8px' }}>
        <g stroke="#e2e8f0" stroke-width="1">
          {Array.from({length: 11}).map((_, i) => <line key={`x-${i}`} x1="-100" y1={-100 + i*20} x2="100" y2={-100 + i*20} />)}
          {Array.from({length: 11}).map((_, i) => <line key={`y-${i}`} x1={-100 + i*20} y1="-100" x2={-100 + i*20} y2="100" />)}
        </g>
        <line x1="-100" y1="0" x2="100" y2="0" stroke="#1e293b" stroke-width="2" />
        <line x1="0" y1="-100" x2="0" y2="100" stroke="#1e293b" stroke-width="2" />
        
        <polygon points="-40,-60 40,-60 40,-100 -40,-100" fill="#eff6ff" stroke="#3b82f6" stroke-width="2" opacity="0.4" />
        <circle cx="0" cy="-20" r="3" fill="#ef4444" />
        
        {/* Image C': (1, 0), (-1, 0), (-1, -1), (1, -1) -> SVG coords */}
        <polygon points="20,0 -20,0 -20,20 20,20" fill="#f0fdf4" stroke="#10b981" stroke-width="2" />
        <text x="0" y="15" font-weight="bold" fill="#10b981" text-anchor="middle" font-size="10">C'</text>
        
        {/* Ray Lines */}
        <line x1="-40" y1="-100" x2="20" y2="20" stroke="#94a3b8" stroke-dasharray="2,2" stroke-width="1.5" />
        <line x1="40" y1="-100" x2="-20" y2="20" stroke="#94a3b8" stroke-dasharray="2,2" stroke-width="1.5" />
      </svg>
    </div>

    <Steps>
      1. **Part a:** Draw a light ray from the corner $(-2, 5)$ straight through the centre $(0,1)$. The vector from the centre $(0,1)$ to the corner $(-2, 5)$ is **Left 2, Up 4**.
      2. **Part b:** Multiply this movement by the Scale Factor $-\frac{1}{2}$. This reverses the direction and halves the distance! The new movement from the centre is **Right 1, Down 2**. This lands at $(1, -1)$.
      3. **Part c:** Repeat for all corners. The final shape will be upside down, on the opposite side of the centre, and exactly half the original size.
    </Steps>

  </TabItem>

</Tabs>

<SmartAIGenerator topic="Applying negative fractional enlargements from a specific centre" />

<Tabs>

  <TabItem label="📝 Q4: Inverse & Combined Transformations">
    Shape $P$ is mapped onto Shape $Q$ by a translation of $\begin{pmatrix} -3 \\ 5 \end{pmatrix}$, followed by an enlargement of scale factor $2$ with centre $(1, 1)$. 
    
    Describe fully the transformations needed to map Shape $Q$ **back** to Shape $P$.
    
    a) State the inverse of the enlargement.
    
    b) State the inverse of the translation.
    
    c) Write out the full sequence of inverse transformations in the correct operational order.
    
  </TabItem>

  <TabItem label="✅ Solution 4">
    <Steps>
      1. **Part a:** Reverse the enlargement first. The original was SF $2$ from centre $(1,1)$. The inverse is an **Enlargement, scale factor $\frac{1}{2}$, centre $(1,1)$**.
      2. **Part b:** Reverse the translation. The original was vector $\begin{pmatrix} -3 \\ 5 \end{pmatrix}$. The inverse is a **Translation by vector $\begin{pmatrix} 3 \\ -5 \end{pmatrix}$**. (Invert the signs).
      3. **Part c:** To reverse a sequence of transformations, you must work backwards through the sequence (like putting on socks then shoes -> take off shoes then socks). Apply the inverse enlargement first, followed by the inverse translation.
    </Steps>

  </TabItem>

</Tabs>

<SmartAIGenerator topic="Combined transformations and applying inverse transformations" />

<Tabs>

  <TabItem label="📝 Q5: Applying a Vector Translation">
    Translate Triangle $D$ by the column vector $\begin{pmatrix} 5 \\ -4 \end{pmatrix}$.
    
    <div style={{ display: 'flex', justifyContent: 'center', margin: '1rem 0' }}>
      <svg viewBox="-100 -100 200 200" width="100%" maxWidth="300px" style={{ background: '#f8fafc', borderRadius: '8px' }}>
        <g stroke="#e2e8f0" stroke-width="1">
          {Array.from({length: 11}).map((_, i) => <line key={`x-${i}`} x1="-100" y1={-100 + i*20} x2="100" y2={-100 + i*20} />)}
          {Array.from({length: 11}).map((_, i) => <line key={`y-${i}`} x1={-100 + i*20} y1="-100" x2={-100 + i*20} y2="100" />)}
        </g>
        <line x1="-100" y1="0" x2="100" y2="0" stroke="#1e293b" stroke-width="2" />
        <line x1="0" y1="-100" x2="0" y2="100" stroke="#1e293b" stroke-width="2" />
        
        {/* Object D: (-4, 1), (-2, 1), (-4, 4) */}
        <polygon points="-80,-20 -40,-20 -80,-80" fill="#eff6ff" stroke="#3b82f6" stroke-width="2" />
        <text x="-60" y="-30" font-weight="bold" fill="#3b82f6" text-anchor="middle">D</text>
      </svg>
    </div>

    a) State the meaning of the top and bottom numbers in the translation vector.

    b) Calculate the new coordinates of the vertex $(-4, 1)$ after the translation.

    c) Draw the final Image $D'$ on the grid.

  </TabItem>

  <TabItem label="✅ Solution 5">
    <div style={{ display: 'flex', justifyContent: 'center', margin: '1rem 0' }}>
      <svg viewBox="-100 -100 200 200" width="100%" maxWidth="300px" style={{ background: '#f8fafc', borderRadius: '8px' }}>
        <g stroke="#e2e8f0" stroke-width="1">
          {Array.from({length: 11}).map((_, i) => <line key={`x-${i}`} x1="-100" y1={-100 + i*20} x2="100" y2={-100 + i*20} />)}
          {Array.from({length: 11}).map((_, i) => <line key={`y-${i}`} x1={-100 + i*20} y1="-100" x2={-100 + i*20} y2="100" />)}
        </g>
        <line x1="-100" y1="0" x2="100" y2="0" stroke="#1e293b" stroke-width="2" />
        <line x1="0" y1="-100" x2="0" y2="100" stroke="#1e293b" stroke-width="2" />
        
        <polygon points="-80,-20 -40,-20 -80,-80" fill="#eff6ff" stroke="#3b82f6" stroke-width="2" opacity="0.4" />
        
        {/* Image D': (1, -3), (3, -3), (1, 0) */}
        <polygon points="20,60 60,60 20,0" fill="#f0fdf4" stroke="#10b981" stroke-width="2" />
        <text x="35" y="45" font-weight="bold" fill="#10b981" text-anchor="middle">D'</text>

        <path d="M -80 -20 L 20 -20 L 20 60" fill="none" stroke="#f59e0b" stroke-width="2" stroke-dasharray="4,4" />
      </svg>
    </div>

    <Steps>
      1. **Part a:** The top number $5$ means move $5$ units right. The bottom number $-4$ means move $4$ units down.
      2. **Part b:** Starting at $(-4, 1)$, moving right $5$ gives $x = 1$. Moving down $4$ gives $y = -3$. The new coordinate is $(1, -3)$.
      3. **Part c:** Apply this same movement to all vertices to draw the congruent shape $D'$.
    </Steps>

  </TabItem>

</Tabs>

<SmartAIGenerator topic="Translating shapes using column vectors" />

<Tabs>

  <TabItem label="📝 Q6: Identifying an Enlargement">
    Rectangle $E$ has been mapped onto Rectangle $E'$.
    
    <div style={{ display: 'flex', justifyContent: 'center', margin: '1rem 0' }}>
      <svg viewBox="-100 -100 200 200" width="100%" maxWidth="300px" style={{ background: '#f8fafc', borderRadius: '8px' }}>
        <g stroke="#e2e8f0" stroke-width="1">
          {Array.from({length: 11}).map((_, i) => <line key={`x-${i}`} x1="-100" y1={-100 + i*20} x2="100" y2={-100 + i*20} />)}
          {Array.from({length: 11}).map((_, i) => <line key={`y-${i}`} x1={-100 + i*20} y1="-100" x2={-100 + i*20} y2="100" />)}
        </g>
        <line x1="-100" y1="0" x2="100" y2="0" stroke="#1e293b" stroke-width="2" />
        <line x1="0" y1="-100" x2="0" y2="100" stroke="#1e293b" stroke-width="2" />
        
        {/* Object E: (1, 1) to (2, 2) */}
        <polygon points="20,-20 40,-20 40,-40 20,-40" fill="#eff6ff" stroke="#3b82f6" stroke-width="2" />
        <text x="30" y="-25" font-weight="bold" fill="#3b82f6" text-anchor="middle" font-size="10">E</text>

        {/* Image E': (2, 2) to (4, 4) */}
        <polygon points="40,-40 80,-40 80,-80 40,-80" fill="#f0fdf4" stroke="#10b981" stroke-width="2" />
        <text x="60" y="-55" font-weight="bold" fill="#10b981" text-anchor="middle" font-size="10">E'</text>
      </svg>
    </div>

    a) Compare the side lengths of $E$ and $E'$ to calculate the scale factor.

    b) Draw ray lines connecting corresponding vertices of $E'$ and $E$. Extend them backwards to find the intersection point.

    c) State the coordinates of the centre of enlargement.

  </TabItem>

  <TabItem label="✅ Solution 6">
    <div style={{ display: 'flex', justifyContent: 'center', margin: '1rem 0' }}>
      <svg viewBox="-100 -100 200 200" width="100%" maxWidth="300px" style={{ background: '#f8fafc', borderRadius: '8px' }}>
        <g stroke="#e2e8f0" stroke-width="1">
          {Array.from({length: 11}).map((_, i) => <line key={`x-${i}`} x1="-100" y1={-100 + i*20} x2="100" y2={-100 + i*20} />)}
          {Array.from({length: 11}).map((_, i) => <line key={`y-${i}`} x1={-100 + i*20} y1="-100" x2={-100 + i*20} y2="100" />)}
        </g>
        <line x1="-100" y1="0" x2="100" y2="0" stroke="#1e293b" stroke-width="2" />
        <line x1="0" y1="-100" x2="0" y2="100" stroke="#1e293b" stroke-width="2" />
        
        <polygon points="20,-20 40,-20 40,-40 20,-40" fill="#eff6ff" stroke="#3b82f6" stroke-width="2" opacity="0.4" />
        <polygon points="40,-40 80,-40 80,-80 40,-80" fill="#f0fdf4" stroke="#10b981" stroke-width="2" opacity="0.4"/>
        
        {/* Ray Lines */}
        <line x1="0" y1="0" x2="80" y2="-80" stroke="#ef4444" stroke-dasharray="2,2" stroke-width="1.5" />
        <line x1="0" y1="0" x2="80" y2="-40" stroke="#ef4444" stroke-dasharray="2,2" stroke-width="1.5" />
        <line x1="0" y1="0" x2="40" y2="-80" stroke="#ef4444" stroke-dasharray="2,2" stroke-width="1.5" />
        <circle cx="0" cy="0" r="3" fill="#ef4444" />
      </svg>
    </div>

    <Steps>
      1. **Part a:** The base of $E$ is $1$ unit long. The base of $E'$ is $2$ units long. The Scale Factor is $2$.
      2. **Part b:** Connect the top-right corner of $E'$ to the top-right corner of $E$ and continue the line. Do the same for the other corners.
      3. **Part c:** All ray lines intersect at the origin $(0, 0)$. This is the centre of enlargement.
    </Steps>

  </TabItem>

</Tabs>

<SmartAIGenerator topic="Finding the centre and scale factor of an enlargement" />

<Tabs>

  <TabItem label="📝 Q7: Identifying Rotation Parameters">
    Triangle $F$ maps onto Triangle $F'$.
    
    <div style={{ display: 'flex', justifyContent: 'center', margin: '1rem 0' }}>
      <svg viewBox="-100 -100 200 200" width="100%" maxWidth="300px" style={{ background: '#f8fafc', borderRadius: '8px' }}>
        <g stroke="#e2e8f0" stroke-width="1">
          {Array.from({length: 11}).map((_, i) => <line key={`x-${i}`} x1="-100" y1={-100 + i*20} x2="100" y2={-100 + i*20} />)}
          {Array.from({length: 11}).map((_, i) => <line key={`y-${i}`} x1={-100 + i*20} y1="-100" x2={-100 + i*20} y2="100" />)}
        </g>
        <line x1="-100" y1="0" x2="100" y2="0" stroke="#1e293b" stroke-width="2" />
        <line x1="0" y1="-100" x2="0" y2="100" stroke="#1e293b" stroke-width="2" />
        
        {/* Object F: (1, 1), (4, 1), (1, 3) */}
        <polygon points="20,-20 80,-20 20,-60" fill="#eff6ff" stroke="#3b82f6" stroke-width="2" />
        <text x="35" y="-25" font-weight="bold" fill="#3b82f6" text-anchor="middle" font-size="10">F</text>

        {/* Image F': (-1, 1), (-1, 4), (-3, 1) */}
        <polygon points="-20,-20 -20,-80 -60,-20" fill="#f0fdf4" stroke="#10b981" stroke-width="2" />
        <text x="-35" y="-25" font-weight="bold" fill="#10b981" text-anchor="middle" font-size="10">F'</text>
      </svg>
    </div>

    a) Visually estimate the angle and direction of rotation.

    b) Test the origin $(0,0)$ as a potential centre by checking if the vector to a point on $F$ rotates correctly to the corresponding point on $F'$.

    c) Write the full description of this single transformation.

  </TabItem>

  <TabItem label="✅ Solution 7">
    <Steps>
      1. **Part a:** The shape has turned a quarter-turn to the left, which is $90^\circ$ anticlockwise.
      2. **Part b:** The vector from $(0,0)$ to the right-angle corner of $F$ is $(1, 1)$. Rotating this $90^\circ$ anticlockwise gives $(-1, 1)$, which perfectly matches the right-angle corner of $F'$. The origin is the correct centre.
      3. **Part c:** Rotation, $90^\circ$ anticlockwise, centre $(0, 0)$.
    </Steps>

  </TabItem>

</Tabs>

<SmartAIGenerator topic="Describing rotations and finding the centre of rotation" />

<Tabs>

  <TabItem label="📝 Q8: Sequential Transformations & Coordinates">
    Point $P$ has coordinates $(2, -3)$. It undergoes a sequence of transformations:
    1. A reflection in the $x$-axis.
    2. A translation by the vector $\begin{pmatrix} -4 \\ 2 \end{pmatrix}$.
    3. An enlargement by scale factor $3$, centre $(0, 0)$.
    
    a) Find the coordinates of $P$ after the reflection.

    b) Find the coordinates of $P$ after the translation.

    c) Find the final coordinates of $P$ after the enlargement.

  </TabItem>

  <TabItem label="✅ Solution 8">
    <Steps>
      1. **Part a:** Reflecting $(2, -3)$ in the $x$-axis flips the $y$-coordinate. New point is $(2, 3)$.
      2. **Part b:** Translating $(2, 3)$ by $\begin{pmatrix} -4 \\ 2 \end{pmatrix}$ means subtracting $4$ from $x$ and adding $2$ to $y$. New point is $(-2, 5)$.
      3. **Part c:** Enlarging $(-2, 5)$ by scale factor $3$ from the origin means multiplying both coordinates by $3$. The final coordinates are $(-6, 15)$.
    </Steps>

  </TabItem>

</Tabs>

<SmartAIGenerator topic="Tracking coordinate changes through multiple sequential transformations" />