---
title: 3.4 Finding a Quadratic Function
description: Learn how to construct the equation of a quadratic graph using its vertex, x-intercepts, and given coordinates.
---

In previous chapters, you learned how to extract key features from a given quadratic equation. In this section, we work in reverse: you will act as a mathematical detective, using visual clues from a graph (like its turning point or where it crosses the axes) to figure out its original equation.

---

## 1. Using the Vertex Form

Every quadratic function can be written in **vertex form**:
$$y = a(x - h)^2 + k$$

This form is incredibly powerful because the coordinates of the vertex (the turning point) are exactly **$(h, k)$**. 

<div style={{ display: 'flex', justifyContent: 'center', margin: '2rem 0', verticalAlign: 'middle' }}>
  <svg viewBox="0 0 400 250" width="100%" maxWidth="600px" style={{ background: '#f8fafc', borderRadius: '8px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
    <g stroke="#cbd5e1" stroke-width="2">
      <line x1="0" y1="150" x2="400" y2="150" />
      <polygon points="392,146 400,150 392,154" fill="#cbd5e1" stroke-width="0" />
      <line x1="200" y1="250" x2="200" y2="0" />
      <polygon points="196,8 200,0 204,8" fill="#cbd5e1" stroke-width="0" />
    </g>
    <text x="385" y="140" font-size="12" fill="#64748b" font-family="sans-serif" font-style="italic">x</text>
    <text x="210" y="15" font-size="12" fill="#64748b" font-family="sans-serif" font-style="italic">y</text>
    <text x="185" y="165" font-size="10" fill="#64748b" font-family="sans-serif">0</text>

    {/* Quadratic: y = (x - 2)^2 + 1. Vertex at (2, 1). Passing through (0, 5). Math Scale: 1 unit = 20px. Origin at (200, 150) */}
    <polyline fill="none" stroke="#d946ef" stroke-width="3" points={Array.from({ length: 401 }, (_, i) => {
      const x = i;
      const mathX = (x - 200) / 20;
      const mathY = Math.pow(mathX - 2, 2) + 1;
      return `${x},${150 - mathY * 20}`;
    }).join(' ')} />

    {/* Vertex at (2, 1) -> Pixel (240, 130) */}
    <circle cx="240" cy="130" r="5" fill="#d946ef" />
    <line x1="247" y1="130" x2="280" y2="130" stroke="#d946ef" stroke-width="1.5" stroke-dasharray="3 3" />
    <text x="285" y="126" font-size="12" fill="#d946ef" font-family="sans-serif" font-weight="bold">Vertex (h, k)</text>
    <text x="285" y="141" font-size="11" fill="#d946ef" font-family="sans-serif">(2, 1)</text>

    {/* Another point at (0, 5) -> Pixel (200, 50) */}
    <circle cx="200" cy="50" r="5" fill="#1e293b" />
    <line x1="193" y1="50" x2="140" y2="50" stroke="#1e293b" stroke-width="1.5" stroke-dasharray="3 3" />
    <text x="50" y="46" font-size="12" fill="#1e293b" font-family="sans-serif" font-weight="bold">Another Point</text>
    <text x="50" y="61" font-size="11" fill="#1e293b" font-family="sans-serif">(0, 5)</text>
  </svg>
</div>

### Finding the Equation Given the Vertex and One Point
If you are given the vertex and exactly one other coordinate on the curve, you can find the full equation.

**Example:** A parabola has a vertex at $(2, 1)$ and passes through the point $(0, 5)$. Find its equation.
1. **Substitute the vertex $(h, k)$:** Here, $h = 2$ and $k = 1$.
   $$y = a(x - 2)^2 + 1$$
2. **Substitute the other point $(x, y)$ to find $a$:**
   The point is $(0, 5)$, so plug in $x = 0$ and $y = 5$.
   $$
   \begin{aligned}
   5 &= a(0 - 2)^2 + 1 \\
   5 &= a(-2)^2 + 1 \\
   5 &= 4a + 1 \\
   4 &= 4a \\
   a &= 1
   \end{aligned}
   $$
3. **Write the final equation:**
   $$y = 1(x - 2)^2 + 1 \implies y = (x - 2)^2 + 1$$

<Aside type="caution" title="The Negative Sign Trap">
Notice the minus sign in $y = a(x - h)^2 + k$. If your vertex has a negative x-coordinate, say $(-3, 4)$, the equation becomes $y = a(x - (-3))^2 + 4$, which simplifies to $y = a(x + 3)^2 + 4$. The sign inside the bracket always looks like the *opposite* of the vertex's x-coordinate!
</Aside>

---

## 2. Using the x-intercepts Form

If a graph crosses the x-axis, those crossing points are called **x-intercepts** or **roots**. If you know both roots, let's call them $p$ and $q$, you can build the quadratic using the **factored form**:
$$y = a(x - p)(x - q)$$

<div style={{ display: 'flex', justifyContent: 'center', margin: '2rem 0', verticalAlign: 'middle' }}>
  <svg viewBox="0 0 400 250" width="100%" maxWidth="600px" style={{ background: '#f8fafc', borderRadius: '8px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
    <g stroke="#cbd5e1" stroke-width="2">
      <line x1="0" y1="100" x2="400" y2="100" />
      <polygon points="392,96 400,100 392,104" fill="#cbd5e1" stroke-width="0" />
      <line x1="200" y1="250" x2="200" y2="0" />
      <polygon points="196,8 200,0 204,8" fill="#cbd5e1" stroke-width="0" />
    </g>
    <text x="385" y="90" font-size="12" fill="#64748b" font-family="sans-serif" font-style="italic">x</text>
    <text x="210" y="15" font-size="12" fill="#64748b" font-family="sans-serif" font-style="italic">y</text>
    <text x="185" y="115" font-size="10" fill="#64748b" font-family="sans-serif">0</text>

    {/* Quadratic: y = (x + 1)(x - 3) = x^2 - 2x - 3. Roots at -1, 3. Point at (1, -4). Math Scale: 1 unit = 30px. Origin at (200, 100) */}
    <polyline fill="none" stroke="#f59e0b" stroke-width="3" points={Array.from({ length: 401 }, (_, i) => {
      const x = i;
      const mathX = (x - 200) / 30;
      const mathY = (mathX + 1) * (mathX - 3);
      return `${x},${100 - mathY * 30}`;
    }).join(' ')} />

    {/* Roots at (-1, 0) and (3, 0) -> Pixels (170, 100) and (290, 100) */}
    <circle cx="170" cy="100" r="5" fill="#f59e0b" />
    <circle cx="290" cy="100" r="5" fill="#f59e0b" />
    <line x1="170" y1="93" x2="170" y2="60" stroke="#f59e0b" stroke-width="1.5" stroke-dasharray="3 3" />
    <line x1="290" y1="93" x2="290" y2="60" stroke="#f59e0b" stroke-width="1.5" stroke-dasharray="3 3" />
    <text x="170" y="50" text-anchor="middle" font-size="12" fill="#f59e0b" font-family="sans-serif" font-weight="bold">Root p</text>
    <text x="170" y="35" text-anchor="middle" font-size="11" fill="#f59e0b" font-family="sans-serif">(-1, 0)</text>
    <text x="290" y="50" text-anchor="middle" font-size="12" fill="#f59e0b" font-family="sans-serif" font-weight="bold">Root q</text>
    <text x="290" y="35" text-anchor="middle" font-size="11" fill="#f59e0b" font-family="sans-serif">(3, 0)</text>

    {/* Another point at (1, -4) -> Pixel (230, 220) */}
    <circle cx="230" cy="220" r="5" fill="#1e293b" />
    <line x1="237" y1="220" x2="270" y2="220" stroke="#1e293b" stroke-width="1.5" stroke-dasharray="3 3" />
    <text x="275" y="216" font-size="12" fill="#1e293b" font-family="sans-serif" font-weight="bold">Another Point</text>
    <text x="275" y="231" font-size="11" fill="#1e293b" font-family="sans-serif">(1, -4)</text>
  </svg>
</div>

### Finding the Equation Given the x-intercepts and One Point
If you are given the two roots and one extra coordinate on the curve, use the factored form.

**Example:** A quadratic graph crosses the x-axis at $x = -1$ and $x = 3$. It passes through the point $(1, -4)$. Find its equation.
1. **Substitute the roots $p$ and $q$:**
   Here, $p = -1$ and $q = 3$.
   $$
   \begin{aligned}
   y &= a(x - (-1))(x - 3) \\
   y &= a(x + 1)(x - 3)
   \end{aligned}
   $$
2. **Substitute the other point $(x, y)$ to find $a$:**
   The point is $(1, -4)$, so plug in $x = 1$ and $y = -4$.
   $$
   \begin{aligned}
   -4 &= a(1 + 1)(1 - 3) \\
   -4 &= a(2)(-2) \\
   -4 &= -4a \\
   a &= 1
   \end{aligned}
   $$
3. **Write the final equation:**
   $$y = 1(x + 1)(x - 3) \implies y = (x + 1)(x - 3)$$
   *(You can expand this to $y = x^2 - 2x - 3$ if the question asks for the form $ax^2 + bx + c$.)*

---

## 3. The Special Case: When $a = 1$

Often, an exam question will explicitly tell you that the function is of the form $y = x^2 + bx + c$. Because there is no number written in front of the $x^2$, this immediately tells you that **$a = 1$**.

This saves you half the work! You do not need a secondary point to substitute. You simply plug your vertex $(h, k)$ into $y = (x - h)^2 + k$, or your roots into $y = (x - p)(x - q)$, and expand the brackets to find your final answer.

<SteveTip title="Expanding Vertex Form Quickly">
If a question tells you $y = x^2 + bx + c$ has a vertex at $(4, -3)$, write $y = (x - 4)^2 - 3$. 
Expand carefully using $(A - B)^2 = A^2 - 2AB + B^2$:
$$
\begin{aligned}
y &= (x^2 - 8x + 16) - 3 \\
y &= x^2 - 8x + 13
\end{aligned}
$$
You have successfully found the coefficients without doing any messy simultaneous equations!
</SteveTip>

---

## 4. Practice Problems

<Tabs>
  <TabItem label="📝 Question 1: Given Vertex and a Point">
    The turning point of a quadratic graph is $(3, 4)$. The graph crosses the y-axis at $(0, -14)$.
    Find the equation of the quadratic in the form $y = a(x - h)^2 + k$.
  </TabItem>
  <TabItem label="✅ Solution 1">
    1. The vertex is $(3, 4)$, so $h = 3$ and $k = 4$.
       $$y = a(x - 3)^2 + 4$$
    2. The graph passes through $(0, -14)$. Substitute $x=0, y=-14$:
       $$
       \begin{aligned}
       -14 &= a(0 - 3)^2 + 4 \\
       -14 &= 9a + 4 \\
       -18 &= 9a \\
       a &= -2
       \end{aligned}
       $$
    3. The equation is:
       **$$y = -2(x - 3)^2 + 4$$**
  </TabItem>
</Tabs>

<AIGenerator course="IGCSE" storageKey="igcse_math_history" topic="Finding the equation of a quadratic function given its vertex and one other point" difficulty="IGCSE Extended" client:load />

<Tabs>
  <TabItem label="📝 Question 2: Given Roots and a Point">
    A quadratic curve has x-intercepts at $x = -2$ and $x = 5$. It passes through the point $(4, 18)$.
    Find the equation of the curve in the form $y = ax^2 + bx + c$.
  </TabItem>
  <TabItem label="✅ Solution 2">
    1. Use the factored form with roots $-2$ and $5$:
       $$
       \begin{aligned}
       y &= a(x - (-2))(x - 5) \\
       y &= a(x + 2)(x - 5)
       \end{aligned}
       $$
    2. Substitute the point $(4, 18)$ to find $a$:
       $$
       \begin{aligned}
       18 &= a(4 + 2)(4 - 5) \\
       18 &= a(6)(-1) \\
       18 &= -6a \\
       a &= -3
       \end{aligned}
       $$
    3. Write the equation and expand to the requested form:
       $$
       \begin{aligned}
       y &= -3(x + 2)(x - 5) \\
       y &= -3(x^2 - 5x + 2x - 10) \\
       y &= -3(x^2 - 3x - 10) \\
       y &= -3x^2 + 9x + 30
       \end{aligned}
       $$
  </TabItem>
</Tabs>

<AIGenerator course="IGCSE" storageKey="igcse_math_history" topic="Constructing quadratic equations from x-intercepts and an extra coordinate point" difficulty="IGCSE Extended" client:load />

<Tabs>
  <TabItem label="📝 Question 3: The a = 1 Shortcut">
    The graph of $y = x^2 + px + q$ has a minimum point at $(-5, -2)$. 
    Find the values of $p$ and $q$.
  </TabItem>
  <TabItem label="✅ Solution 3">
    1. The form $y = x^2 + px + q$ means that $a = 1$.
    2. The vertex is $(-5, -2)$. Put this directly into vertex form $y = (x - h)^2 + k$:
       $$
       \begin{aligned}
       y &= (x - (-5))^2 - 2 \\
       y &= (x + 5)^2 - 2
       \end{aligned}
       $$
    3. Expand the brackets:
       $$
       \begin{aligned}
       y &= (x^2 + 10x + 25) - 2 \\
       y &= x^2 + 10x + 23
       \end{aligned}
       $$
    4. Compare this to $y = x^2 + px + q$:
       **$p = 10, q = 23$**
  </TabItem>
</Tabs>

<AIGenerator course="IGCSE" storageKey="igcse_math_history" topic="Finding the coefficients of a quadratic equation when the vertex is given and a=1" difficulty="IGCSE Core" client:load />