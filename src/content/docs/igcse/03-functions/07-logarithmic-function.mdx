---
title: 3.7 The Logarithmic Function
description: Master the basics of logarithms, understand their inverse relationship with exponential functions, and solve real-world growth equations for IGCSE 0607.
---

Just as subtraction is the opposite of addition, and division is the opposite of multiplication, **logarithms are the mathematical opposite of exponentials**. 

When you have an equation like $10^x = 1000$, it is easy to see that $x = 3$. But what if the equation is $10^x = 50$? We need a mathematical tool to "undo" the exponent and solve for $x$. That tool is the logarithm.

### A Brief History of the Logarithm
Before calculators existed, scientists and astronomers spent months doing tedious long multiplication by hand. In the late 16th century, a Scottish mathematician named **John Napier** spent over 20 years manually calculating millions of geometric ratios. In 1614, he published his invention: logarithms. Napier's brilliant system allowed mathematicians to turn horrific multiplication problems into simple addition problems, changing the course of science forever!

---

## 1. The Inverse Relationship

A logarithm simply asks the question: *"What power do I need to raise the base to, in order to get this number?"*

The fundamental rule you must memorize is the conversion between exponential form and logarithmic form:
$$y = a^x \iff x = \log_a y$$

Here, $a$ is the **base**. Notice that the base of the exponent becomes the base of the logarithm. 

### Equivalent Statements
To get comfortable with logs, you must be able to translate fluently between exponential and logarithmic forms.

| Exponential Form | Logarithmic Form | How to read it |
| :--- | :--- | :--- |
| $10^2 = 100$ | $\log_{10} 100 = 2$ | "The power of 10 that gives 100 is 2" |
| $10^3 = 1000$ | $\log_{10} 1000 = 3$ | "The power of 10 that gives 1000 is 3" |
| $10^0 = 1$ | $\log_{10} 1 = 0$ | "The power of 10 that gives 1 is 0" |
| $10^{-1} = 0.1$ | $\log_{10} 0.1 = -1$ | "The power of 10 that gives 0.1 is -1" |
| $2^5 = 32$ | $\log_2 32 = 5$ | "The power of 2 that gives 32 is 5" |

<SteveTip title="The Default Base 10 & The TI-Nspire">
In the IGCSE 0607 syllabus, **all logarithms are assumed to be base 10 unless stated otherwise**. If an exam paper writes $\log 50$, it specifically means $\log_{10} 50$.

**Calculator Warning:** When you press **Ctrl + $10^x$** on your TI-Nspire, it brings up the template $\log_{\Box}(\Box)$. Unlike standard written math, the calculator **does not** assume base 10! You *must* manually type `10` into the small base box, otherwise, it will return an error or evaluate algebraically. 
</SteveTip>

---

## 2. The Cancellation Properties

Because logarithms and exponentials are inverse functions, they effectively "cancel" each other out when nested together. These two cancellation properties are incredibly useful for solving algebraic equations:

1. **Logarithm of an Exponential:** $$\log_a(a^x) = x$$
   *(Example: $\log_{10}(10^7) = 7$)*
   
2. **Exponential of a Logarithm:** $$a^{\log_a(x)} = x$$
   *(Example: $10^{\log_{10}(45)} = 45$)*

---

## 3. The Logarithmic Graph

Because logarithmic functions are the exact inverse of exponential functions, their graphs are mirror images of each other, reflected perfectly across the diagonal line $y = x$.

Let's look at the graphs of $y = 10^x$ and its inverse, $y = \log_{10} x$:

<div style={{ display: 'flex', justifyContent: 'center', margin: '2rem 0', verticalAlign: 'middle' }}>
  <svg viewBox="0 0 400 400" width="100%" maxWidth="500px" style={{ background: '#f8fafc', borderRadius: '8px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
    <g stroke="#cbd5e1" stroke-width="2">
      <line x1="0" y1="200" x2="400" y2="200" />
      <polygon points="392,196 400,200 392,204" fill="#cbd5e1" stroke-width="0" />
      <line x1="200" y1="400" x2="200" y2="0" />
      <polygon points="196,8 200,0 204,8" fill="#cbd5e1" stroke-width="0" />
    </g>
    <text x="385" y="190" font-size="12" fill="#64748b" font-family="sans-serif" font-style="italic">x</text>
    <text x="210" y="15" font-size="12" fill="#64748b" font-family="sans-serif" font-style="italic">y</text>
    <text x="185" y="215" font-size="10" fill="#64748b" font-family="sans-serif">0</text>

    {/* Line of reflection: y = x */}
    <line x1="0" y1="400" x2="400" y2="0" stroke="#94a3b8" stroke-width="2" stroke-dasharray="6 6" />
    <text x="360" y="30" font-size="12" fill="#94a3b8" font-family="sans-serif" font-style="italic">y = x</text>

    {/* Exponential: y = 10^x (Green) */}
    <polyline fill="none" stroke="#10b981" stroke-width="3" points={Array.from({ length: 401 }, (_, i) => {
      const x = i;
      const mathX = (x - 200) / 40;
      let mathY = Math.pow(10, mathX);
      let pixelY = 200 - mathY * 40;
      return `${x},${pixelY}`;
    }).filter(p => parseFloat(p.split(',')[1]) >= -50).join(' ')} />
    <text x="110" y="50" font-size="14" font-weight="bold" fill="#047857" font-family="sans-serif">y = 10<tspan dy="-5" font-size="11">x</tspan></text>

    {/* Logarithmic: y = log_10(x) (Blue) */}
    <polyline fill="none" stroke="#3b82f6" stroke-width="3" points={Array.from({ length: 401 }, (_, i) => {
      const pixelY = i;
      const mathY = (200 - pixelY) / 40;
      const mathX = Math.pow(10, mathY); 
      const pixelX = 200 + mathX * 40;
      return `${pixelX},${pixelY}`;
    }).filter(p => parseFloat(p.split(',')[0]) <= 400).join(' ')} />
    <text x="280" y="250" font-size="14" font-weight="bold" fill="#2563eb" font-family="sans-serif">y = log(x)</text>
  </svg>
</div>

**Key properties to notice:**
* The exponential graph has a horizontal asymptote at $y = 0$ (the x-axis) and a y-intercept at **$(0, 1)$**.
* The logarithmic graph has a vertical asymptote at $x = 0$ (the y-axis) and an x-intercept at **$(1, 0)$**. Notice how it dives steeply downwards as $x$ approaches zero.
* You **cannot** take the logarithm of zero or a negative number. The domain is strictly $x > 0$.

---

## 4. Solving Exponential Equations

When the unknown variable is trapped in the power (e.g., $a^x = b$), you can solve it by taking the logarithm (base 10) of both sides. 

Using the rules of logarithms, the power $x$ drops down to the front, giving us the standard solution formula:
$$a^x = b \implies x = \frac{\log b}{\log a}$$

<Aside type="note" title="Example Walkthrough">
**Solve for $x$: $3^x = 45$**

1. Apply the formula: $x = \frac{\log 45}{\log 3}$
2. Type this into your GDC (remembering to input base 10!).
3. $x \approx 3.46$ (to 3 significant figures).
</Aside>

---

## 5. Real-World Contexts: Growth and Decay

Logarithms are incredibly useful in finance and science when you need to figure out **how long** something takes to grow or decay.

### Compound Interest (Growth)
If you want to know how many years it will take for an investment to reach a certain value, you are solving an exponential equation.

**Example:**
500 dollars is invested at a compound interest rate of 4% per year. How many full years will it take for the investment to exceed 800 dollars?

1. Set up the compound interest formula: $500 \times \left(1 + \frac{4}{100}\right)^n = 800$
2. Divide by 500: $1.04^n = 1.6$
3. Use the logarithm rule: $n = \frac{\log 1.6}{\log 1.04}$
4. Calculate: $n \approx 12.0003$
5. Since it asks for *full* years to *exceed* 800, the answer is **13 years**.

### Depreciation (Decay)
The exact same logic applies when things lose value over time.

**Example:**
A new car is bought for 25,000 dollars. It depreciates (loses value) at a rate of 15% per year. How many years will it take for its value to drop to exactly 10,000 dollars?

1. Set up the decay formula (using subtraction instead of addition): $25000 \times \left(1 - \frac{15}{100}\right)^n = 10000$
2. Simplify the bracket and divide by 25,000: $0.85^n = 0.4$
3. Use the logarithm rule: $n = \frac{\log 0.4}{\log 0.85}$
4. Calculate: $n \approx 5.64$
5. **Answer:** It will take **5.64 years**.

---

## 6. Solving Log Equations from a Graph

Sometimes an exam question will give you a complicated logarithmic or exponential equation to solve, like $10^{x - 1} = 5x + 2$. Instead of using algebra, you should use your GDC.

Treat both sides of the equation as separate functions and plot them:
* $f_1(x) = 10^{x - 1}$
* $f_2(x) = 5x + 2$

<div style={{ display: 'flex', justifyContent: 'center', margin: '2rem 0', verticalAlign: 'middle' }}>
  <svg viewBox="0 0 400 300" width="100%" maxWidth="500px" style={{ background: '#f8fafc', borderRadius: '8px', border: '1px solid #e2e8f0', overflow: 'hidden' }}>
    <g stroke="#cbd5e1" stroke-width="2">
      <line x1="0" y1="250" x2="400" y2="250" />
      <polygon points="392,246 400,250 392,254" fill="#cbd5e1" stroke-width="0" />
      <line x1="100" y1="300" x2="100" y2="0" />
      <polygon points="96,8 100,0 104,8" fill="#cbd5e1" stroke-width="0" />
    </g>
    <text x="385" y="240" font-size="12" fill="#64748b" font-family="sans-serif" font-style="italic">x</text>
    <text x="110" y="15" font-size="12" fill="#64748b" font-family="sans-serif" font-style="italic">y</text>
    <text x="85" y="265" font-size="10" fill="#64748b" font-family="sans-serif">0</text>

    {/* Linear Function: y = 5x + 2 (Red) */}
    <line x1="20" y1="295" x2="340" y2="-5" stroke="#ef4444" stroke-width="2" />
    <text x="150" y="110" font-size="14" font-weight="bold" fill="#dc2626" font-family="sans-serif">y = 5x + 2</text>

    {/* Exponential Function: y = 10^(x-1) (Green) */}
    <polyline fill="none" stroke="#10b981" stroke-width="3" points={Array.from({ length: 301 }, (_, i) => {
      const x = i + 20;
      const mathX = (x - 100) / 80;
      let mathY = Math.pow(10, mathX - 1);
      let pixelY = 250 - mathY * 15;
      return `${x},${pixelY}`;
    }).filter(p => parseFloat(p.split(',')[1]) >= -50).join(' ')} />
    <text x="280" y="150" font-size="14" font-weight="bold" fill="#047857" font-family="sans-serif">y = 10<tspan dy="-5" font-size="11">x-1</tspan></text>

    {/* Intersection Point */}
    <circle cx="268" cy="63" r="5" fill="#f59e0b" />
    <text x="280" y="68" font-size="12" font-weight="bold" fill="#d97706" font-family="sans-serif"> (2.10, 12.5)</text>
    
    {/* Dashed line to x-axis */}
    <line x1="268" y1="63" x2="268" y2="250" stroke="#f59e0b" stroke-width="1.5" stroke-dasharray="4 4" />
    <text x="255" y="265" font-size="12" font-weight="bold" fill="#d97706" font-family="sans-serif">2.10</text>
  </svg>
</div>

Use **Menu $\rightarrow$ Analyze Graph $\rightarrow$ Intersection** on your calculator to find the point where the two lines cross. The $x$-coordinate of this intersection ($x \approx 2.10$) is the solution to your equation!

---

## 7. Practice Problems

<Tabs>
  <TabItem label="📝 Question 1: Translating and Evaluating">
    a) Write the exponential equation $2^7 = 128$ in logarithmic form.
    
    b) Write the logarithmic equation $\log_5 625 = 4$ in exponential form.
    
    c) Evaluate $\log_{10} 100000$ without using a calculator.
  </TabItem>
  <TabItem label="✅ Solution 1">
    **a)**
    <Steps>
      1. Use the rule $a^x = y \iff \log_a y = x$.
      2. The base is $2$, the exponent is $7$, and the result is $128$.
      3. **$\log_2 128 = 7$**
    </Steps>
    
    **b)**
    <Steps>
      1. Reverse the rule: the base is $5$, the logarithm (power) is $4$, and the result is $625$.
      2. **$5^4 = 625$**
    </Steps>

    **c)**
    <Steps>
      1. Ask yourself: "10 to what power equals 100,000?"
      2. Count the zeros: $10^5 = 100000$.
      3. **$5$**
    </Steps>
  </TabItem>
</Tabs>

<SmartAIGenerator topic="Translating between exponential and logarithmic forms and basic evaluation without a calculator" />

<Tabs>
  <TabItem label="📝 Question 2: Cancellation Properties">
    a) Simplify the expression $10^{\log_{10}(5x)}$.
    
    b) Simplify the expression $\log_3(3^{y + 2})$.
    
    c) Use cancellation to solve the equation $10^{\log_{10}(2k)} = 18$.
  </TabItem>
  <TabItem label="✅ Solution 2">
    **a)**
    <Steps>
      1. Use the property $a^{\log_a(x)} = x$.
      2. The base 10 exponent and base 10 logarithm cancel.
      3. **$5x$**
    </Steps>

    **b)**
    <Steps>
      1. Use the property $\log_a(a^x) = x$.
      2. The base 3 logarithm and base 3 exponent cancel.
      3. **$y + 2$**
    </Steps>

    **c)**
    <Steps>
      1. Cancel the exponential and logarithm on the left side: $2k = 18$.
      2. Divide both sides by $2$.
      3. **$k = 9$**
    </Steps>
  </TabItem>
</Tabs>

<SmartAIGenerator topic="Simplifying algebraic expressions and solving equations using logarithm cancellation properties" />

<Tabs>
  <TabItem label="📝 Question 3: Solving Exponential Equations">
    a) Solve the equation $7^x = 300$. Give your answer correct to 3 significant figures.
    
    b) Solve the equation $4 \times 10^x = 850$. Give your answer correct to 3 significant figures.
  </TabItem>
  <TabItem label="✅ Solution 3">
    **a)**
    <Steps>
      1. Apply the standard solution formula: $x = \frac{\log 300}{\log 7}$.
      2. Calculate using your GDC: $x \approx 2.931...$
      3. **$x = 2.93$**
    </Steps>

    **b)**
    <Steps>
      1. First, isolate the exponential part by dividing both sides by $4$.
      2. $10^x = 212.5$
      3. Convert to logarithmic form: $x = \log_{10} 212.5$.
      4. Calculate: $x \approx 2.327...$
      5. **$x = 2.33$**
    </Steps>
  </TabItem>
</Tabs>

<SmartAIGenerator topic="Solving basic and multistep exponential equations using logarithms" />

<Tabs>
  <TabItem label="📝 Question 4: Solving Logarithmic Equations">
    a) Solve for $x$: $\log_{10}(x) = 4.2$. Give your answer correct to 1 decimal place.
    
    b) Solve for $x$: $\log_{10}(2x - 5) = 2$.
  </TabItem>
  <TabItem label="✅ Solution 4">
    **a)**
    <Steps>
      1. Convert the logarithmic equation back into exponential form.
      2. $x = 10^{4.2}$
      3. Calculate: $x = 15848.931...$
      4. **$x = 15848.9$**
    </Steps>

    **b)**
    <Steps>
      1. Convert to exponential form: $2x - 5 = 10^2$.
      2. Simplify: $2x - 5 = 100$.
      3. Add $5$ to both sides: $2x = 105$.
      4. Divide by $2$: $x = 52.5$.
      5. **$x = 52.5$**
    </Steps>
  </TabItem>
</Tabs>

<SmartAIGenerator topic="Solving logarithmic equations by converting them back into exponential form" />

<Tabs>
  <TabItem label="📝 Question 5: Compound Interest Growth">
    800 dollars is invested in an account paying 5% compound interest per year. Let $n$ be the number of years.
    
    a) Write down an exponential equation to find when the investment will reach 1500 dollars.
    
    b) Solve your equation from part (a) to find the exact value of $n$ (to 2 decimal places).
    
    c) State the number of *full* years required for the investment to exceed 1500 dollars.
  </TabItem>
  <TabItem label="✅ Solution 5">
    **a)**
    <Steps>
      1. The multiplier is $1 + \frac{5}{100} = 1.05$.
      2. Initial amount $\times$ (multiplier)$^n$ $=$ Final amount.
      3. **$800 \times 1.05^n = 1500$**
    </Steps>

    **b)**
    <Steps>
      1. Divide both sides by 800: $1.05^n = 1.875$.
      2. Apply the log rule: $n = \frac{\log 1.875}{\log 1.05}$.
      3. Calculate: $n = 12.883...$
      4. **$n = 12.88$**
    </Steps>

    **c)**
    <Steps>
      1. The threshold is crossed during the 12th year.
      2. To ensure it has fully *exceeded* the amount, we round up to the next complete year.
      3. **13 full years**
    </Steps>
  </TabItem>
</Tabs>

<SmartAIGenerator topic="Using logarithms to solve compound interest growth word problems" />

<Tabs>
  <TabItem label="📝 Question 6: Exponential Decay (Depreciation)">
    A machine is purchased for 12,000 dollars. Its value depreciates at a rate of 12% each year. 
    
    a) Write down an equation to find how many years ($n$) it takes for the value to drop to exactly 5,000 dollars.
    
    b) Solve your equation to find $n$, correct to 2 decimal places.
  </TabItem>
  <TabItem label="✅ Solution 6">
    **a)**
    <Steps>
      1. The multiplier for a 12% decrease is $1 - 0.12 = 0.88$.
      2. Initial value $\times$ (multiplier)$^n$ $=$ Final value.
      3. **$12000 \times 0.88^n = 5000$**
    </Steps>

    **b)**
    <Steps>
      1. Divide both sides by 12,000: $0.88^n = \frac{5}{12}$.
      2. Apply the log rule: $n = \frac{\log(5/12)}{\log 0.88}$.
      3. Calculate: $n = 6.848...$
      4. **$n = 6.85$**
    </Steps>
  </TabItem>
</Tabs>

<SmartAIGenerator topic="Using logarithms to solve exponential decay and depreciation word problems" />

<Tabs>
  <TabItem label="📝 Question 7: Graphical Solutions">
    Consider the functions $f(x) = 10^{0.5x}$ and $g(x) = 8 - x$.
    
    a) If you were to find the intersection of these two graphs, write down the algebraic equation you would be solving.
    
    b) By graphing both functions on your GDC, find the $x$-coordinate of their intersection point. Give your answer to 3 significant figures.
  </TabItem>
  <TabItem label="✅ Solution 7">
    **a)**
    <Steps>
      1. At the intersection, the $y$-values are equal.
      2. Set $f(x)$ equal to $g(x)$.
      3. **$10^{0.5x} = 8 - x$**
    </Steps>

    **b)**
    <Steps>
      1. Plot $y = 10^{0.5x}$ as your first function on the GDC.
      2. Plot $y = 8 - x$ as your second function.
      3. Use the **Menu $\rightarrow$ Analyze Graph $\rightarrow$ Intersection** tool.
      4. The calculator shows the intersection point at roughly $(1.581..., 6.418...)$.
      5. The $x$-coordinate solves the equation.
      6. **$x = 1.58$**
    </Steps>
  </TabItem>
</Tabs>

<SmartAIGenerator topic="Solving complex exponential equations using graphing calculator intersection tools" />